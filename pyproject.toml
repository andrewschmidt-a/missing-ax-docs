[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "axdocs"
authors = [{name = "Andrew Schmidt", email = "andrew@nemcrunchers.dev"}]
dynamic = ["version", "description"]
